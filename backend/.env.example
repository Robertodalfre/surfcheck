PORT=4000
CACHE_TTL_MIN=20
REDIS_URL=
NODE_ENV=development

# Open-Meteo Marine models (opcional). Exemplo: OM_MARINE_MODELS=gfs_wave
# Se vazio, usa "best match" padrão da Open-Meteo.
OM_MARINE_MODELS=gfs_wave
